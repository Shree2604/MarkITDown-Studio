fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# markitdown[all] pulls in every optional converter (incl. audio/youtube
# transcription, which drag in larger ML deps). If you only need office docs
# + PDFs, swap this line for:
#   markitdown[pdf,docx,pptx,xlsx,outlook]==0.1.6
markitdown[all]==0.1.6
